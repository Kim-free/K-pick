package com.example.kpick.mission.repository;

import com.example.kpick.mission.domain.MissionSuggestionOption;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MissionSuggestionOptionRepository extends JpaRepository<MissionSuggestionOption, Long> {
}
